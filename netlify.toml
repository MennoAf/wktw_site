[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Netlify defaults to Pretty URLs (strips trailing slashes) — matches Astro's
# trailingSlash: 'never' setting. No redirect rules needed.
