---
import { features } from '../data/features';

const navItems = [
  { label: 'The Get Right', href: '/the-get-right' },
  { label: 'Insights', href: '/insights' },
  { label: 'Proof', href: '/proof' },
  { label: 'About', href: '/about' },
];

const currentPath = Astro.url.pathname;
---

<header class="sticky top-0 z-50 bg-bg-deep/90 backdrop-blur-sm">
  <nav class="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between">
    <!-- Logo -->
    <a href="/" class="text-primary-text font-heading font-bold text-xl tracking-tight hover:text-gilt transition-colors">
      WKTW
    </a>

    <!-- Desktop Nav -->
    <div class="hidden md:flex items-center gap-8">
      {navItems.map((item) => (
        <a
          href={item.href}
          class:list={[
            'text-sm font-medium transition-colors',
            currentPath.startsWith(item.href)
              ? 'text-accent'
              : 'text-secondary-text hover:text-primary-text',
          ]}
        >
          {item.label}
        </a>
      ))}
      {features.search && (
        <a
          href="/search"
          aria-label="Search"
          class:list={[
            'inline-flex items-center min-h-[44px] text-sm font-medium transition-colors',
            currentPath.startsWith('/search')
              ? 'text-accent'
              : 'text-secondary-text hover:text-primary-text',
          ]}
        >
          Search
        </a>
      )}
      <a href="/contact" class="btn btn-gold btn-sm">
        Talk to a Founder
      </a>
    </div>

    <!-- Mobile Menu Button — 48×48 tap target (WCAG 2.5.8) -->
    <button
      id="mobile-menu-btn"
      class="md:hidden text-primary-text inline-flex items-center justify-center min-h-[48px] min-w-[48px]"
      aria-label="Toggle menu"
      aria-expanded="false"
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path id="menu-icon-open" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        <path id="menu-icon-close" class="hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 6l12 12M6 18L18 6" />
      </svg>
    </button>
  </nav>

  <!-- Gradient hairline: the header's bottom edge reads as a gilt rule, not a flat border. -->
  <div class="hairline absolute bottom-0 inset-x-0" aria-hidden="true"></div>

  <!-- Mobile Menu — starts hidden + inert so its links leave the a11y/focus tree
       until opened (WL-06). Links are 48px tap targets (WL-04). -->
  <div id="mobile-menu" class="hidden md:hidden border-t border-border-subtle bg-bg-deep" inert aria-hidden="true">
    <div class="px-6 py-4 flex flex-col gap-2">
      {navItems.map((item) => (
        <a
          href={item.href}
          class:list={[
            'text-base font-medium transition-colors flex items-center min-h-[48px]',
            currentPath.startsWith(item.href)
              ? 'text-accent'
              : 'text-secondary-text hover:text-primary-text',
          ]}
        >
          {item.label}
        </a>
      ))}
      {features.search && (
        <a
          href="/search"
          class:list={[
            'text-base font-medium transition-colors flex items-center min-h-[48px]',
            currentPath.startsWith('/search')
              ? 'text-accent'
              : 'text-secondary-text hover:text-primary-text',
          ]}
        >
          Search
        </a>
      )}
      <a href="/contact" class="btn btn-gold min-h-[48px] justify-center text-sm">
        Talk to a Founder
      </a>
    </div>
  </div>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  const iconOpen = document.getElementById('menu-icon-open');
  const iconClose = document.getElementById('menu-icon-close');
  btn?.addEventListener('click', () => {
    const willOpen = btn.getAttribute('aria-expanded') !== 'true';
    btn.setAttribute('aria-expanded', String(willOpen));
    menu?.classList.toggle('hidden', !willOpen);
    iconOpen?.classList.toggle('hidden', willOpen);
    iconClose?.classList.toggle('hidden', !willOpen);
    if (menu) {
      // When closed, take the menu out of the a11y/focus tree (WL-06).
      menu.toggleAttribute('inert', !willOpen);
      menu.setAttribute('aria-hidden', String(!willOpen));
    }
  });
</script>
