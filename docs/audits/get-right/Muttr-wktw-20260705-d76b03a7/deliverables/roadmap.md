# Client Roadmap: Digital Experience Enhancements

This roadmap outlines planned enhancements to your digital presence, organized into 30, 60, and 90-day sprints, followed by a backlog of future initiatives. Each item includes a concise description, estimated effort, cost, priority level, and expected impact on key performance indicators.

---

## Quick Wins MVP

The following fixes deliver the highest impact for the least effort — start here.

### 1. No GA4 or equivalent full-featured analytics — conversion tracking and enhanced measurement absent

- **Effort:** Quick Win
- **Cost:** Low
- **Severity:** Critical
- **Expected impact:** The site currently has zero conversion data. This fix creates the measurement foundation: every contact form submission, email link click, and CTA interaction becomes a countable, attributable event. Without this, no marketing spend, content change, or UX improvement can be evaluated for effectiveness.

### 2. Article about audit failures does not link to the firm's own audit service page

- **Effort:** Quick Win
- **Cost:** Low
- **Severity:** High
- **Expected impact:** Readers arriving via search on audit-failure queries currently reach a persuasion endpoint with no in-content pathway to the service page. The CTA placed at the agitation-to-conclusion transition intercepts intent at its peak — the moment the reader has accepted the problem framing and is primed for a solution. Without this link, the only conversion path is a separate navigation action after the article ends, which requires the reader to sustain intent across a context switch. The inline CTA eliminates that gap.

### 3. Escalation Resolution: CSP Absence Confirmed with High Confidence

- **Effort:** Quick Win
- **Cost:** Low
- **Severity:** High
- **Expected impact:** CSP is the only browser-enforced mechanism that restricts which scripts can execute on a page. Without it, any XSS vector — reflected, stored, or DOM-based — executes arbitrary JavaScript with full access to cookies, DOM, and user session. Adding CSP creates a whitelist that causes the browser to block any script not matching an approved origin or hash, neutralizing the primary payload delivery mechanism for XSS attacks. This closes the single remaining gap in an otherwise complete security header configuration.

### 4. Escalation review: CSP confirmed absent — prescan-2-1 upheld

- **Effort:** Quick Win
- **Cost:** Low
- **Severity:** High
- **Expected impact:** CSP eliminates the primary XSS exploitation vector by restricting script execution to explicitly whitelisted origins and hashed inline scripts. Any injected script — via DOM manipulation, reflected input, or compromised third-party — will be blocked by the browser and logged as a CSP violation. This converts the current open-execution environment (any script runs) into a deny-by-default environment (only inventoried scripts run).

### 5. Privacy policy link at 17px height is critically undersized — consent context makes this high-impact

- **Effort:** Quick Win
- **Cost:** Low
- **Severity:** High
- **Expected impact:** The privacy policy link is a legally required element of informed consent under GDPR Article 7 and CCPA. At 17px height (35% of the WCAG 2.5.8 minimum), it is functionally inaccessible on touch devices, which undermines the legal defensibility of any consent collected through this banner. Expanding to 48px makes the link reliably tappable, restoring the 'informed' prerequisite of consent collection.

---
## Sprint 30: Immediate Impact & Foundational Fixes

This sprint focuses on high-priority items that deliver immediate value, address critical user experience and compliance issues, and lay the groundwork for future optimizations.

| Priority | Title | Effort | Cost | Expected Impact |
| :------- | :---- | :----- | :--- | :-------------- |
| Critical | Add a primary above-the-fold CTA on the homepage that creates a direct, frictionless conversion path to the contact form. This is a template-level architectural fix: the homepage must actively route users toward the client's stated primary KPI (contact_form submission) rather than passively existing as a brand landing page. Three coordinated changes are required: (1) a hero-section CTA button linking to the contact page or a contact anchor, (2) a persistent 'Contact' link in the sticky navigation, and (3) an inline contextual prompt in the value proposition section. All three must be implemented together — any single element in isolation is insufficient because users scan pages non-linearly and may enter the viewport at different scroll depths. | quick_win | low | The current state creates a zero-signal homepage: a user arriving on the highest-traffic entry point has no above-the-fold prompt directing them toward the client's primary KPI (contact_form submission). The fix creates three independent discovery paths — hero CTA, persistent nav, and mid-page contextual prompt — each targeting users at different scroll depths and decision stages. The technical mechanism is straightforward: a user who cannot see a path to the contact form cannot convert via |
| Critical | Replace the broken custom consent banner with a self-contained Astro component that renders visibly by default (no JS required to show), wires up Google Consent Mode v2 grant/deny on button click, persists the decision to localStorage, and fixes all related accessibility failures (contrast, touch targets, accessible names). The banner must be visible without JavaScript as a legal safety net — JS enhances it (hiding after interaction, firing Consent Mode updates) but is not required for display. | quick_win | low | Google Consent Mode currently defaults to 'denied' and can never be updated to 'granted' because the banner never renders. Fixing this restores the consent grant pathway, which unblocks all consent-gated analytics (GA4, Google Ads conversion tracking, remarketing audiences). The site transitions from zero analytics data collection to normal collection for users who accept. |
| High | Expand all 16 undersized touch targets on the About page to meet WCAG 2.5.8's 48×48 CSS pixel minimum, prioritized by conversion path position: (1) primary contact/inquiry CTA, (2) primary mobile navigation links, (3) team member social icon links, (4) cookie consent banner action buttons. The fix must expand tap areas without altering visual design — using CSS padding and ::after pseudo-element overlays — and must propagate to every instance of the team grid and mobile navigation components, not just the About page. | quick_win | low | The primary contact/inquiry CTA is the terminal action in the About page mobile conversion path. If this button is among the 16 undersized targets, mobile visitors attempting to tap it will experience mis-taps, accidental dismissals, or repeated failed attempts — each failure increases the probability of abandonment before the intent is completed. Expanding the CTA to 48×48px eliminates this physical barrier to conversion completion. The mechanism is direct: a tap tha |
| High | Enforce a minimum 48×48 CSS pixel interactive surface on all touch targets across the contact page template — including CTA buttons, icon-only links (phone, email, social), form field labels, and inline anchor links — without altering their visual footprint. The fix uses a layered strategy: (1) a scoped CSS utility class that expands the tap surface via min-height/min-width and padding floors, (2) a ::before pseudo-element tap-target expander for icon-only controls where visual resizing is unacceptable, (3) a form-field association audit to ensure labels are block-level and correctly linked, and (4) a platform-level audit of the global button/link component to prevent recurrence on other pages. All changes must be WCAG 2.5.8 compliant, visually non-destructive, and backwards-compatible to iOS Safari 14 / Android Chrome 88. | quick_win | low | The mechanism is direct: a user who cannot reliably activate a tap target on a contact form — whether a field label, a CTA button, or an inline consent link — will either retry (adding friction and frustration) or abandon. Unlike desktop, mobile provides no hover state to confirm target boundaries before committing a tap. The failure is silent — no error, no feedback — the user simply fails to initiate the intended action. Bringing all eight failing elements to t |
| High | Add a persistent, accessible tel: link contact affordance to the base template so that mobile users have a frictionless, single-tap path to initiate a call. This is a template-level fix that propagates to every page inheriting the base layout. The implementation has three layers: (1) a tel: link in the primary navigation/header for all viewports, (2) a mobile-only sticky call-to-action bar that persists as the user scrolls, and (3) a structured data telephone property on the Organization schema so search engines surface the number in rich results and Google's mobile SERP call button. All three layers are complementary — the header link serves desktop and mobile users who are already engaged, the sticky bar captures high-intent mobile users mid-scroll who have not yet committed to contacting, and the schema layer captures users who have not yet landed on the site. | quick_win | low | The current state requires a mobile user who wants to call to: (1) visually locate a phone number in plain text, (2) long-press to copy it, (3) exit the browser, (4) open the phone dialer, (5) paste or manually type the number, and (6) initiate the call. This is a 5-6 step friction sequence. A tel: link collapses this to a single tap. For a consulting or coaching brand where a phone call is a high-intent conversion event, eliminating this friction sequence directly |
| High | Establish a two-layer internal linking system: (1) a hand-authored contextual CTA block embedded in the audit-failures article body at the point of maximum persuasion, and (2) a reusable Astro component with a content-map config that enforces topic-to-service-page linking for all current and future articles — preventing the authoring gap from recurring without relying on author discipline. | quick_win | low | Readers arriving via search on audit-failure queries currently reach a persuasion endpoint with no in-content pathway to the service page. The CTA placed at the agitation-to-conclusion transition intercepts intent at its peak — the moment the reader has accepted the problem framing and is primed for a solution. Without this link, the only conversion path is a separate navigation action after the article ends, which requires the reader to sustain intent across a conte |
| High | Add a tel: link alongside the existing mailto: link in the global footer component, visible only on touch/mobile viewports, so prospects at peak intent (post-case-study, post-services read) can initiate contact in one tap without switching to an email client. | quick_win | low | A tel: link eliminates the composition step entirely — one tap opens the native dialer with the number pre-filled. Email requires opening a mail client, composing a subject and body, and waiting for a reply. On high-intent pages (case studies, services), where a prospect has just finished evaluating the consultancy, the cognitive load of email composition is a friction insertion at the moment of highest conversion probability. Removing that friction makes immediate c |
| High | Add a Content-Security-Policy response header to the Netlify configuration that restricts script execution to same-origin and plausible.io, covering the site's 7 inline scripts via hashes and its single external script dependency. | quick_win | low | CSP is the only browser-enforced mechanism that restricts which scripts can execute on a page. Without it, any XSS vector — reflected, stored, or DOM-based — executes arbitrary JavaScript with full access to cookies, DOM, and user session. Adding CSP creates a whitelist that causes the browser to block any script not matching an approved origin or hash, neutralizing the primary payload delivery mechanism for XSS attacks. This closes the single remaining gap in an otherwise |
| High | Create an enforced meta tag contract in the Astro base layout that guarantees every page ships with complete Open Graph and Twitter Card tags, using frontmatter values with validated fallback defaults, and failing the build when high-value page types omit required fields. | quick_win | low | Pages missing og:image render as plain text links on LinkedIn, Facebook, Slack, and iMessage — no thumbnail, no rich card. Adding complete OG tags enables rich preview cards, which are the primary visual differentiator between a link that gets clicked and one that gets scrolled past. The mechanism is direct: platforms fetch OG tags at share-time, and missing tags produce degraded or blank previews. |
| High | Add a Content-Security-Policy response header to the Netlify deployment, restricting script execution to same-origin and plausible.io, with SHA-256 hashes for all 7 inline script blocks. This closes the XSS injection surface on every page. | quick_win | low | CSP eliminates the primary XSS exploitation vector by restricting script execution to explicitly whitelisted origins and hashed inline scripts. Any injected script — via DOM manipulation, reflected input, or compromised third-party — will be blocked by the browser and logged as a CSP violation. This converts the current open-execution environment (any script runs) into a deny-by-default environment (only inventoried scripts run). |
| High | Implement Plausible custom event tracking for all conversion actions (contact form submission, mailto: clicks, CTA button clicks) and configure a first-party proxy to defeat ad blockers. No new analytics platform needed — leverage the existing Plausible investment. | quick_win | low | The site currently has zero conversion data. This fix creates the measurement foundation: every contact form submission, email link click, and CTA interaction becomes a countable, attributable event. Without this, no marketing spend, content change, or UX improvement can be evaluated for effectiveness. |
| High | Add a build-time broken link checker to the Astro/Netlify CI pipeline that verifies all 14 internal hrefs resolve to real generated routes before deployment completes. The checker runs as a post-build step, fails the Netlify build on any broken link, and produces a structured report. No runtime code is added to the site. | quick_win | low | Every deploy that would ship a broken internal link is blocked before it reaches production. The /contact/ route — the site's sole conversion destination — cannot silently break without failing the build. This eliminates the failure mode where a CMS edit or route rename severs the inquiry path without any visible error. |
| High | Add programmatic accessible names to two unnamed interactive components: (1) the Netlify hidden form-name field on /contact/ — suppress it from the accessibility tree entirely via aria-hidden and tabindex; (2) the shared template interactive element on / (and all pages) — most likely an icon-only navigation toggle or bare SVG anchor — add an explicit accessible name via aria-label or visually-hidden text. Both fixes are scoped to their respective Astro components to prevent regressions elsewhere. | quick_win | low | WCAG 4.1.2 (Name, Role, Value) is a Level A criterion — the baseline conformance level. Unnamed interactive elements cause screen readers to announce 'unlabeled' or skip the element entirely, making the contact form and navigation non-functional for blind and low-vision users. ADA Title III web accessibility lawsuits are well-documented and frequently target contact forms and navigation as primary barriers. Resolving these violations moves the site from non-conf |
| High | Introduce a reusable InlineCTA Astro component and embed it in two positions on the About page: (1) immediately after the H1 hero statement for high-intent visitors who arrive pre-sold, and (2) at the existing below-fold position for low-intent visitors who complete the narrative arc. Then deploy the same component to service pages and the /proof/ page to close the site-wide conversion gap. The component must require zero JavaScript for its primary function — it is a styled anchor, not a widget — so it degrades gracefully to a plain link in any environment. | quick_win | low | High-intent visitors (referral, word-of-mouth, prior research) currently encounter no conversion affordance until they scroll past the full narrative. The above-fold CTA eliminates this scroll requirement for that cohort. The mechanism: reducing the number of required steps between intent and action directly reduces abandonment at the intent stage. The below-fold CTA is preserved, so low-intent visitors who need the full narrative are unaffected. |
| High | Ensure the Netlify hidden form-name input is explicitly typed as hidden (type="hidden"), which removes it from the accessibility tree, keyboard tab order, and visual rendering per HTML spec — eliminating the WCAG 4.1.2 and 1.3.1 violation without breaking Netlify form submission. | quick_win | low | Eliminates the WCAG 4.1.2 (Name, Role, Value